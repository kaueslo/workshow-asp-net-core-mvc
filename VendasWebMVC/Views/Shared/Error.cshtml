@model VendasWebMVC.Models.ViewModels.ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">@ViewData["Title"]</h1>
<h2 class="text-danger">@Model.Mensagem</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo Dev</h3>
<p>
    Trocar para o ambiente de <strong>Desenvolvimento</strong> irá mostrar maiores detalhes.
</p>
<p>
   O Ambiente de <strong>Desenvolvimento</strong> não deve ser implantado nos aplicativos que já estão em produção, pois pode acarretar em exibição de alguns dados.
</p>
